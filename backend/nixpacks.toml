[phases.setup]
nixPkgs = ["python312", "cairo", "pango", "gdk-pixbuf", "shared-mime-info", "fontconfig", "freetype", "zlib", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn folioforge_api.wsgi:application --bind [::]:${PORT:-8080}"
